## 1. Implementation

- [x] 1.1 Add settings icon link to mobile header right-side action bar in `Header.svelte`, wrapped in `{#if authState.isAuthenticated}`, between the dark-mode toggle and user button

## 2. Verification

- [x] 2.1 Run lint (`bun run lint`) and confirm no errors
- [x] 2.2 Run build (`bun run build`) and confirm no errors

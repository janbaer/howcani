[install]
# Use exact versions for reproducibility
exact = true

[test]
# Test configuration
coverage = true

[define]
"process.env.HOWCANI_JWT_SECRET" = "'dev-jwt-secret'"

[serve.static]
plugins = ["bun-plugin-svelte"]
